# Çalışma zamanı bağımlılıkları (API sunucusu).
# Eğitim ve test bağımlılıkları requirements-dev.txt'te — imaja girmezler.
pandas>=2.0
numpy>=1.26
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
sqlalchemy>=2.0    # ilan persistansı (yerel: SQLite, yayın: DATABASE_URL)
psycopg[binary]>=3.1  # Postgres sürücüsü (yalnız yayında kullanılır)
python-multipart>=0.0.9  # fotoğraf yükleme (multipart form)
requests>=2.31     # OTP e-postası (Brevo) + scripts/fetch_transit.py

# Model servis etme (yükleme + tahmin)
lightgbm>=4.0
joblib>=1.3
