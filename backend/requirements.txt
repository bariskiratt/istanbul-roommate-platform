pandas>=2.0
numpy>=1.26
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
sqlalchemy>=2.0    # ilan persistansı (yerel: SQLite, yayın: DATABASE_URL)
psycopg[binary]>=3.1  # Postgres sürücüsü (yalnız yayında kullanılır)
python-multipart>=0.0.9  # fotoğraf yükleme (multipart form)

# Testler
pytest>=8.0
httpx>=0.27        # FastAPI TestClient
json-repair>=0.25
requests>=2.31   # scripts/fetch_transit.py (kendi kök sertifikalarını getirir)

# Adil fiyat modeli (train_model.py)
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.0
joblib>=1.3
